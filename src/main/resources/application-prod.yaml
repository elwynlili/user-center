spring:
  application:
    name: usercenter
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/common_user_center
    username: root
    password: rootroot
server:
  port: 8080
  reactive:
    session:
      timeout: 86400000

